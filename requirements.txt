contourpy==1.3.1
cycler==0.12.1
fonttools==4.55.0
kiwisolver==1.4.7
matplotlib==3.9.2
networkx==3.4.2
numpy==2.1.3
packaging==24.2
pillow==11.0.0
pyparsing==3.2.0
python-dateutil==2.9.0.post0
setuptools==75.6.0
six==1.16.0
tree-sitter==0.23.2
tree-sitter-python==0.23.4

# Neural-network stack. Versions depend on your OS / CUDA, so these are left
# unpinned -- install a build that matches your environment.
torch
torch_geometric

# Shared embedding corpus development backend. The library falls back to exact
# local cosine search if this optional package is unavailable.
faiss-cpu
